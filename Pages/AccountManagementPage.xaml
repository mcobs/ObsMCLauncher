<Page x:Class="ObsMCLauncher.Pages.AccountManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="900"
      KeepAlive="True"
      Background="{DynamicResource BackgroundBrush}"
      UseLayoutRounding="True"
      TextOptions.TextFormattingMode="Display"
      TextOptions.TextRenderingMode="ClearType">

    <Grid>
        <!-- 主内容区域 -->
        <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题区域 -->
            <StackPanel Grid.Row="0">
                <TextBlock Text="账号管理" 
                         FontSize="28" 
                         FontWeight="Bold"
                         Margin="0,0,0,8"/>
                <TextBlock Text="管理您的 Minecraft 账号，支持微软账户和离线模式" 
                         FontSize="14" 
                         Foreground="{DynamicResource TextSecondaryBrush}"
                         Margin="0,0,0,16"/>
            </StackPanel>

            <!-- 账号列表和操作 -->
            <StackPanel Grid.Row="1">
                
                <!-- 添加账号按钮 -->
                <Border Background="{DynamicResource SurfaceBrush}"
                        CornerRadius="10"
                        Padding="20"
                        Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="添加新账号" 
                                     FontSize="18" 
                                     FontWeight="SemiBold"
                                     Margin="0,0,0,5"/>
                            <TextBlock Text="登录微软账户、外置登录或创建离线账号" 
                                     FontSize="13" 
                                     Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>

                        <Button Grid.Column="1"
                              Style="{StaticResource MaterialDesignRaisedButton}"
                              Background="{StaticResource PrimaryBrush}"
                              Margin="0,0,15,0"
                              Padding="16,14"
                              MinWidth="120"
                              Height="48"
                              Click="AddMicrosoftAccount_Click">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="Microsoft" 
                                                       Width="20" 
                                                       Height="20"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"/>
                                <TextBlock Text="微软账户" 
                                         VerticalAlignment="Center"
                                         FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <Button Grid.Column="2"
                              Style="{StaticResource MaterialDesignOutlinedButton}"
                              Padding="16,14"
                              MinWidth="120"
                              Height="48"
                              Margin="0,0,15,0"
                              Click="AddYggdrasilAccount_Click">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="Shield" 
                                                       Width="20" 
                                                       Height="20"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"/>
                                <TextBlock Text="外置登录" 
                                         VerticalAlignment="Center"
                                         FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <Button Grid.Column="3"
                              Style="{StaticResource MaterialDesignOutlinedButton}"
                              Padding="16,14"
                              MinWidth="120"
                              Height="48"
                              Click="AddOfflineAccount_Click">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="AccountOff" 
                                                       Width="20" 
                                                       Height="20"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"/>
                                <TextBlock Text="离线账户" 
                                         VerticalAlignment="Center"
                                         FontSize="14"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </Border>

                <!-- 添加账号输入界面 -->
                <Border x:Name="AddAccountPanel"
                        Background="{StaticResource SurfaceElevatedBrush}"
                        CornerRadius="10"
                        Padding="25"
                        Margin="0,0,0,20"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="添加离线账户" 
                                 FontSize="18" 
                                 FontWeight="SemiBold"
                                 Margin="0,0,0,20"/>
                        
                        <TextBlock Text="用户名" 
                                 FontSize="14" 
                                 Foreground="{DynamicResource TextSecondaryBrush}"
                                 Margin="0,0,0,8"/>
                        
                        <TextBox x:Name="UsernameTextBox"
                               materialDesign:HintAssist.Hint="请输入用户名（3-16个字符）"
                               Style="{StaticResource MaterialDesignOutlinedTextBox}"
                               FontSize="14"
                               MaxLength="16"
                               Margin="0,0,0,20"/>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="取消"
                                  Style="{StaticResource MaterialDesignOutlinedButton}"
                                  Margin="0,0,15,0"
                                  Width="100"
                                  Click="CancelAddAccount_Click"/>
                            <Button Content="确认添加"
                                  Style="{StaticResource MaterialDesignRaisedButton}"
                                  Background="{StaticResource PrimaryBrush}"
                                  Width="100"
                                  Click="ConfirmAddAccount_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 账号列表 -->
                <TextBlock Text="我的账号" 
                         FontSize="20" 
                         FontWeight="SemiBold"
                         Margin="0,0,0,15"/>

                <StackPanel x:Name="AccountListPanel">
                    <!-- 动态生成账号列表 -->
                </StackPanel>

                <!-- 账号类型说明 -->
                <Border Background="{DynamicResource SurfaceBrush}"
                        CornerRadius="10"
                        Padding="25"
                        Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="账号类型说明" 
                                 FontSize="18" 
                                 FontWeight="SemiBold"
                                 Margin="0,0,0,15"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- 微软账户说明 -->
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <Border Background="{DynamicResource SurfaceElevatedBrush}"
                                        CornerRadius="8"
                                        Padding="15">
                                    <StackPanel>
                                        <materialDesign:PackIcon Kind="Microsoft"
                                                               Width="30"
                                                               Height="30"
                                                               Foreground="{DynamicResource PrimaryBrush}"
                                                               Margin="0,0,0,10"/>
                                        <TextBlock Text="微软账户" 
                                                 FontSize="16" 
                                                 FontWeight="SemiBold"
                                                 Margin="0,0,0,5"/>
                                        <TextBlock Text="• 支持正版皮肤和披风" 
                                                 FontSize="12" 
                                                 Foreground="{DynamicResource TextSecondaryBrush}"
                                                 Margin="0,0,0,3"/>
                                        <TextBlock Text="• 可进入正版服务器" 
                                                 FontSize="12" 
                                                 Foreground="{DynamicResource TextSecondaryBrush}"
                                                 Margin="0,0,0,3"/>
                                        <TextBlock Text="• 需要购买正版游戏" 
                                                 FontSize="12" 
                                                 Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <!-- 外置登录说明 -->
                            <StackPanel Grid.Column="1" Margin="10,0,10,0">
                                <Border Background="{DynamicResource SurfaceElevatedBrush}"
                                        CornerRadius="8"
                                        Padding="15">
                                    <StackPanel>
                                        <materialDesign:PackIcon Kind="Shield"
                                                               Width="30"
                                                               Height="30"
                                                               Foreground="#66BB6A"
                                                               Margin="0,0,0,10"/>
                                        <TextBlock Text="外置登录" 
                                                 FontSize="16" 
                                                 FontWeight="SemiBold"
                                                 Margin="0,0,0,5"/>
                                        <TextBlock Text="• 支持第三方认证服务器" 
                                                 FontSize="12" 
                                                 Foreground="{DynamicResource TextSecondaryBrush}"
                                                 Margin="0,0,0,3"/>
                                        <TextBlock Text="• 可加载自定义皮肤披风" 
                                                 FontSize="12" 
                                                 Foreground="{DynamicResource TextSecondaryBrush}"
                                                 Margin="0,0,0,3"/>
                                        <TextBlock Text="• 需要注册外置账号" 
                                                 FontSize="12" 
                                                 Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <!-- 离线账户说明 -->
                            <StackPanel Grid.Column="2" Margin="10,0,0,0">
                                <Border Background="{DynamicResource SurfaceElevatedBrush}"
                                        CornerRadius="8"
                                        Padding="15">
                                    <StackPanel>
                                        <materialDesign:PackIcon Kind="AccountOff"
                                                               Width="30"
                                                               Height="30"
                                                               Foreground="{DynamicResource SecondaryBrush}"
                                                               Margin="0,0,0,10"/>
                                        <TextBlock Text="离线账户" 
                                                 FontSize="16" 
                                                 FontWeight="SemiBold"
                                                 Margin="0,0,0,5"/>
                                        <TextBlock Text="• 无需购买即可使用" 
                                                 FontSize="12" 
                                                 Foreground="{DynamicResource TextSecondaryBrush}"
                                                 Margin="0,0,0,3"/>
                                        <TextBlock Text="• 仅可进入离线服务器" 
                                                 FontSize="12" 
                                                 Foreground="{DynamicResource TextSecondaryBrush}"
                                                 Margin="0,0,0,3"/>
                                        <TextBlock Text="• 自定义任意用户名" 
                                                 FontSize="12" 
                                                 Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
    </Grid>
</Page>

