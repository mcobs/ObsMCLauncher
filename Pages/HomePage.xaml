<Page x:Class="ObsMCLauncher.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="900"
      KeepAlive="True"
      Background="{StaticResource BackgroundBrush}">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

            <!-- 欢迎区域 -->
            <StackPanel Grid.Row="0">
                <TextBlock Text="欢迎使用 Obs MC 启动器" 
                         FontSize="32" 
                         FontWeight="Bold"
                         Foreground="{StaticResource TextBrush}"
                         Margin="0,0,0,10"/>
                <TextBlock Text="让您的 Minecraft 之旅更加轻松愉快" 
                         FontSize="16" 
                         Foreground="#AAAAAA"
                         Margin="0,0,0,30"/>
            </StackPanel>



            <!-- 快速启动区域 -->
            <Border Grid.Row="2"
                    Background="{StaticResource SurfaceBrush}"
                    CornerRadius="8"
                    Padding="20,15"
                    Margin="0,15,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 账号选择 -->
                    <ComboBox Grid.Column="0"
                            x:Name="AccountComboBox"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Center"
                            materialDesign:HintAssist.Hint="选择游戏账号"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}">
                        <!-- 动态加载账号列表 -->
                    </ComboBox>

                    <!-- 版本选择 -->
                    <ComboBox Grid.Column="2"
                            x:Name="VersionComboBox"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Center"
                            materialDesign:HintAssist.Hint="选择游戏版本"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}">
                        <!-- 动态加载版本列表 -->
                    </ComboBox>

                    <!-- 启动按钮 -->
                    <Button Grid.Column="4"
                          x:Name="LaunchButton"
                          Content="启动游戏"
                          Height="40"
                          Width="140"
                          FontSize="14"
                          VerticalAlignment="Center"
                          Style="{StaticResource MaterialDesignRaisedButton}"
                          materialDesign:ButtonAssist.CornerRadius="6"
                          Background="{StaticResource PrimaryBrush}"
                          Click="LaunchButton_Click">
                        <Button.Effect>
                            <DropShadowEffect Color="{Binding Source={StaticResource PrimaryBrush}, Path=Color}" 
                                            BlurRadius="12" 
                                            Opacity="0.2" 
                                            ShadowDepth="0"/>
                        </Button.Effect>
                    </Button>
                </Grid>
            </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>

