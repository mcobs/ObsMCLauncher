<Page x:Class="ObsMCLauncher.Pages.MorePage"
      xmlns:vm="clr-namespace:ObsMCLauncher.ViewModels"
      xmlns:converters="clr-namespace:ObsMCLauncher.Utils.Converters"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="更多"
      Background="Transparent"
      UseLayoutRounding="True"
      TextOptions.TextFormattingMode="Display"
      TextOptions.TextRenderingMode="ClearType">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InvBoolToVis"/>
        <converters:PluginTabToBoolConverter x:Key="PluginTabToBoolConverter"/>

        <!-- 卡片样式 -->
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceElevatedBrush}"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="10" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PluginLeftListItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="Transparent">
                            <ContentPresenter RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="PluginLeftItemTemplate">
            <Border x:Name="ItemCard"
                    CornerRadius="8"
                    Padding="12"
                    BorderThickness="1"
                    Background="{DynamicResource SurfaceElevatedBrush}"
                    BorderBrush="{DynamicResource BorderBrush}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="{Binding Title}"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextBrush}"/>
                        <TextBlock Text="{Binding Meta}"
                                   FontSize="11"
                                   Margin="0,2,0,0"
                                   Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="{Binding MarketActionText}"
                                Padding="10,2"
                                FontSize="12"
                                Margin="0,0,8,0"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Command="{Binding DataContext.InstallFromListCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                CommandParameter="{Binding}"
                                IsEnabled="{Binding MarketActionEnabled}"
                                Visibility="{Binding IsMarket, Converter={StaticResource BoolToVis}}"/>

                        <TextBlock Text="!"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Foreground="#FF4747"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>
                </Grid>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsMouseOver}" Value="True">
                    <Setter TargetName="ItemCard" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                    <Setter TargetName="ItemCard" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                    <Setter TargetName="ItemCard" Property="BorderThickness" Value="2"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <!-- 链接按钮样式 -->
        <Style x:Key="LinkButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 标题样式 -->
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部导航栏 -->
        <Border Grid.Row="0" 
                Background="{DynamicResource SurfaceElevatedBrush}"
                BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource BorderBrush}"
                Padding="24,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <RadioButton x:Name="AboutTab"
                           Content="关于"
                           GroupName="MoreTabs"
                           Style="{StaticResource MaterialDesignTabRadioButton}"
                           Padding="20,8"
                           FontSize="14"
                           IsChecked="True"
                           Checked="Tab_Checked"/>
                
                <RadioButton x:Name="PluginsTab"
                           Content="插件"
                           GroupName="MoreTabs"
                           Style="{StaticResource MaterialDesignTabRadioButton}"
                           Padding="20,8"
                           FontSize="14"
                           Margin="12,0,0,0"
                           Checked="Tab_Checked"/>
                
                <RadioButton x:Name="ScreenshotsTab"
                           Content="截图管理"
                           GroupName="MoreTabs"
                           Style="{StaticResource MaterialDesignTabRadioButton}"
                           Padding="20,8"
                           FontSize="14"
                           Margin="12,0,0,0"
                           Checked="Tab_Checked"/>
                
                <RadioButton x:Name="ServersTab"
                           Content="服务器收藏"
                           GroupName="MoreTabs"
                           Style="{StaticResource MaterialDesignTabRadioButton}"
                           Padding="20,8"
                           FontSize="14"
                           Margin="12,0,0,0"
                           Checked="Tab_Checked"/>
            </StackPanel>
        </Border>

        <!-- 内容区域 -->
        <Grid Grid.Row="1">
            <!-- 关于页面 -->
            <ScrollViewer x:Name="AboutContent"
                        VerticalScrollBarVisibility="Auto" 
                        HorizontalScrollBarVisibility="Disabled"
                        Padding="24"
                        Visibility="Visible">
                <StackPanel MaxWidth="800">
            <!-- Logo和标题 -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel HorizontalAlignment="Center">
                    <!-- Logo SVG -->
                    <svgc:SvgViewbox Source="/Assets/logo.svg" 
                                     Width="140" 
                                     Height="140" 
                                     Margin="0,0,0,24"
                                     Stretch="Uniform"/>

                    <!-- 启动器名称 -->
                    <TextBlock x:Name="AppTitleText"
                             Text="ObsMCLauncher" 
                             FontSize="32" 
                             FontWeight="Bold" 
                             Foreground="{DynamicResource TextBrush}"
                             HorizontalAlignment="Center"
                             Margin="0,0,0,8"
                             Cursor="Hand"
                             MouseLeftButtonDown="AppTitleText_MouseLeftButtonDown"/>
                    
                    <!-- 副标题 -->
                    <TextBlock Text="黑曜石 Minecraft 启动器" 
                             FontSize="16" 
                             Foreground="{DynamicResource TextSecondaryBrush}"
                             HorizontalAlignment="Center"
                             Margin="0,0,0,16"/>
                    
                    <!-- 版本信息 -->
                    <TextBlock x:Name="VersionText"
                             Text="版本 1.0.0"
                             FontSize="14"
                             Foreground="{DynamicResource TextTertiaryBrush}"
                             HorizontalAlignment="Center"
                             Margin="0,0,0,12"/>
                    
                    <!-- 检查更新按钮 -->
                    <Button x:Name="CheckUpdateButton"
                            Click="CheckUpdateButton_Click"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Height="32"
                            Padding="20,0"
                            HorizontalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Update" 
                                                   Width="16" 
                                                   Height="16"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,8,0"/>
                            <TextBlock Text="检查更新" VerticalAlignment="Center" FontSize="13"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <!-- 相关链接 -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="相关链接" Style="{StaticResource SectionTitle}"/>
                    
                    <!-- GitHub链接 -->
                    <Button Style="{StaticResource LinkButton}" Click="OpenGitHub_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔗" FontSize="16" Margin="0,0,8,0"/>
                            <StackPanel>
                                <TextBlock Text="GitHub 开源仓库" FontWeight="SemiBold"/>
                                <TextBlock Text="https://github.com/mcobs/ObsMCLauncher" 
                                         FontSize="12" 
                                         Foreground="{DynamicResource TextTertiaryBrush}"
                                         Margin="0,4,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Button>

                    <!-- 黑曜石论坛 -->
                    <Button Style="{StaticResource LinkButton}" Click="OpenForum_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🌐" FontSize="16" Margin="0,0,8,0"/>
                            <StackPanel>
                                <TextBlock Text="黑曜石论坛" FontWeight="SemiBold"/>
                                <TextBlock Text="https://mcobs.cn/" 
                                         FontSize="12" 
                                         Foreground="{DynamicResource TextTertiaryBrush}"
                                         Margin="0,4,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <!-- 感谢 -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="特别感谢" Style="{StaticResource SectionTitle}"/>
                    
                    <!-- bangbang93 -->
                    <Border Background="{DynamicResource SurfaceBrush}" 
                            CornerRadius="6" 
                            Padding="16" 
                            Margin="0,0,0,12">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="💝" FontSize="18" Margin="0,0,8,0"/>
                                <TextBlock Text="bangbang93" 
                                         FontSize="16" 
                                         FontWeight="SemiBold"
                                         Foreground="{DynamicResource TextBrush}"
                                         VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <TextBlock Text="感谢提供 BMCLAPI 镜像源服务" 
                                     FontSize="14" 
                                     Foreground="{DynamicResource TextSecondaryBrush}"
                                     Margin="26,0,0,12"
                                     TextWrapping="Wrap"/>
                            
                            <Button Style="{StaticResource LinkButton}" 
                                    Click="OpenBangBang93_Click"
                                    Margin="26,0,0,0"
                                    Background="{StaticResource PrimaryBrush}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="❤️" FontSize="14" Margin="0,0,8,0"/>
                                    <TextBlock Text="爱发电赞助" Foreground="{DynamicResource TextBrush}"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>

                    <!-- 图标设计师 -->
                    <Border Background="{DynamicResource SurfaceBrush}" 
                            CornerRadius="6" 
                            Padding="16"
                            Margin="0,0,0,12">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🎨" FontSize="18" Margin="0,0,8,0"/>
                                <TextBlock Text="路过的京局DF7C" 
                                         FontSize="16" 
                                         FontWeight="SemiBold"
                                         Foreground="{DynamicResource TextBrush}"
                                         VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <TextBlock Text="感谢设计本软件的精美图标" 
                                     FontSize="14" 
                                     Foreground="{DynamicResource TextSecondaryBrush}"
                                     Margin="26,8,0,0"
                                     TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <!-- authlib-injector -->
                    <Border Background="{DynamicResource SurfaceBrush}" 
                            CornerRadius="6" 
                            Padding="16">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="🔐" FontSize="18" Margin="0,0,8,0"/>
                                <TextBlock Text="authlib-injector" 
                                         FontSize="16" 
                                         FontWeight="SemiBold"
                                         Foreground="{DynamicResource TextBrush}"
                                         VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <TextBlock Text="感谢提供 Yggdrasil 外置登录支持" 
                                     FontSize="14" 
                                     Foreground="{DynamicResource TextSecondaryBrush}"
                                     Margin="26,0,0,12"
                                     TextWrapping="Wrap"/>
                            
                            <Button Style="{StaticResource LinkButton}" 
                                    Click="OpenAuthlibInjector_Click"
                                    Margin="26,0,0,0"
                                    Background="Transparent"
                                    BorderBrush="{DynamicResource PrimaryBrush}"
                                    BorderThickness="1">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🔗" FontSize="14" Margin="0,0,8,0"/>
                                    <TextBlock Text="访问项目主页" Foreground="{DynamicResource PrimaryBrush}"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- 关于 -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="关于" Style="{StaticResource SectionTitle}"/>
                    
                    <TextBlock Text="ObsMCLauncher 是一款现代化的 Minecraft 启动器，采用 .NET 8.0 和 WPF 技术开发，提供流畅的游戏管理体验。" 
                             FontSize="14" 
                             Foreground="{DynamicResource TextSecondaryBrush}"
                             TextWrapping="Wrap"
                             LineHeight="22"
                             Margin="0,0,0,16"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="开源协议：" 
                                 FontSize="14" 
                                 Foreground="{DynamicResource TextTertiaryBrush}"
                                 Width="80"/>
                        <TextBlock Text="GNU GPL v3.0" 
                                 FontSize="14" 
                                 Foreground="{DynamicResource TextBrush}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="技术栈：" 
                                 FontSize="14" 
                                 Foreground="{DynamicResource TextTertiaryBrush}"
                                 Width="80"/>
                        <TextBlock Text=".NET 8.0 | WPF | C#" 
                                 FontSize="14" 
                                 Foreground="{DynamicResource TextBrush}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 免责声明 -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="⚠️ 免责声明" 
                             Style="{StaticResource SectionTitle}"
                             Foreground="{DynamicResource PrimaryBrush}"/>
                        <!-- 保留黄色强调色 -->
                    
                    <TextBlock FontSize="13" 
                             Foreground="{DynamicResource TextSecondaryBrush}"
                             TextWrapping="Wrap"
                             LineHeight="20">
                        <Run Text="本启动器为第三方工具，与 Mojang Studios 和 Microsoft 无关。"/>
                        <LineBreak/>
                        <Run Text="Minecraft 是 Mojang Studios 的注册商标。"/>
                        <LineBreak/>
                        <Run Text="使用本软件即表示您同意自行承担使用风险。"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- 版权信息 -->
            <TextBlock Text="© 2025 ObsMCLauncher - 使用 ❤️ 和 C# 构建" 
                     FontSize="12" 
                     Foreground="{DynamicResource TextTertiaryBrush}"
                     HorizontalAlignment="Center"
                     Margin="0,8,0,20"/>
                </StackPanel>
            </ScrollViewer>

            <!-- 插件页面 -->
            <!-- 截图管理页面 -->
            <Frame x:Name="ScreenshotsContent"
                 NavigationUIVisibility="Hidden"
                 Visibility="Collapsed"
                 Margin="0"/>
            
            <!-- 服务器收藏页面 -->
            <Frame x:Name="ServersContent"
                 NavigationUIVisibility="Hidden"
                 Visibility="Collapsed"
                 Margin="0"/>
            
            <ScrollViewer x:Name="PluginsContent"
                        VerticalScrollBarVisibility="Auto" 
                        HorizontalScrollBarVisibility="Disabled"
                        Padding="24"
                        Visibility="Collapsed">
                <StackPanel MaxWidth="1100">
                    <Border Style="{StaticResource CardBorder}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Horizontal">
                                    <RadioButton Content="市场"
                                               GroupName="PluginSubTabs"
                                               Style="{StaticResource MaterialDesignTabRadioButton}"
                                               Padding="16,6"
                                               FontSize="13"
                                               IsChecked="{Binding CurrentTab, Mode=TwoWay, Converter={StaticResource PluginTabToBoolConverter}, ConverterParameter=Market}"/>

                                    <RadioButton Content="已安装"
                                               GroupName="PluginSubTabs"
                                               Style="{StaticResource MaterialDesignTabRadioButton}"
                                               Padding="16,6"
                                               FontSize="13"
                                               Margin="12,0,0,0"
                                               IsChecked="{Binding CurrentTab, Mode=TwoWay, Converter={StaticResource PluginTabToBoolConverter}, ConverterParameter=Installed}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                          Height="32"
                                          Padding="12,0"
                                          Command="{Binding RefreshMarketCommand}"
                                          Visibility="{Binding CurrentTab, Converter={StaticResource PluginTabToBoolConverter}, ConverterParameter=Market, ConverterCulture=zh-CN}">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="Refresh" 
                                                                   Width="16" 
                                                                   Height="16"
                                                                   VerticalAlignment="Center"
                                                                   Margin="0,0,6,0"/>
                                            <TextBlock Text="刷新市场" VerticalAlignment="Center" FontSize="13"/>
                                        </StackPanel>
                                    </Button>

                                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                          Height="32"
                                          Padding="12,0"
                                          Margin="10,0,0,0"
                                          Command="{Binding RefreshInstalledCommand}">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="Refresh" 
                                                                   Width="16" 
                                                                   Height="16"
                                                                   VerticalAlignment="Center"
                                                                   Margin="0,0,6,0"/>
                                            <TextBlock Text="刷新已安装" VerticalAlignment="Center" FontSize="13"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Grid>

                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="420"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Grid Grid.Column="0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0" Margin="0,0,0,12"
                                          Visibility="{Binding CurrentTab, Converter={StaticResource PluginTabToBoolConverter}, ConverterParameter=Market, ConverterCulture=zh-CN}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBox materialDesign:HintAssist.Hint="搜索插件..."
                                               materialDesign:HintAssist.FloatingScale="0.75"
                                               Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                               MinHeight="40"
                                               FontSize="14"
                                               Padding="12,8"
                                               VerticalAlignment="Stretch"
                                               VerticalContentAlignment="Center"
                                               Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

                                        <ComboBox Grid.Column="1"
                                                Width="140"
                                                MinHeight="40"
                                                FontSize="14"
                                                Padding="12,8"
                                                Margin="12,0,0,0"
                                                VerticalAlignment="Stretch"
                                                VerticalContentAlignment="Center"
                                                Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                                materialDesign:HintAssist.Hint="分类"
                                                materialDesign:HintAssist.FloatingScale="0.75"
                                                DisplayMemberPath="Name"
                                                ItemsSource="{Binding Categories}"
                                                SelectedItem="{Binding SelectedCategory}"/>
                                    </Grid>

                                    <Border Grid.Row="1"
                                          Background="{DynamicResource SurfaceBrush}"
                                          CornerRadius="6"
                                          Padding="16"
                                          Margin="0,0,0,12"
                                          Visibility="{Binding IsMarketLoading, Converter={StaticResource BoolToVis}}">
                                        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                                       IsIndeterminate="True"
                                                       Width="24"
                                                       Height="24"
                                                       Margin="0,0,12,0"/>
                                            <TextBlock Text="正在加载插件市场..." 
                                                     FontSize="13"
                                                     Foreground="{DynamicResource TextSecondaryBrush}"
                                                     VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Row="1"
                                          Background="{DynamicResource SurfaceBrush}"
                                          CornerRadius="6"
                                          Padding="16"
                                          Margin="0,0,0,12"
                                          Visibility="{Binding MarketError, Converter={StaticResource BoolToVis}}">
                                        <StackPanel HorizontalAlignment="Center">
                                            <materialDesign:PackIcon Kind="AlertCircleOutline" 
                                                                   Width="40" 
                                                                   Height="40"
                                                                   Foreground="{StaticResource PrimaryBrush}"
                                                                   HorizontalAlignment="Center"
                                                                   Margin="0,0,0,10"/>
                                            <TextBlock Text="{Binding MarketError}" 
                                                     FontSize="13"
                                                     Foreground="{DynamicResource TextSecondaryBrush}"
                                                     HorizontalAlignment="Center"
                                                     Margin="0,0,0,8"/>
                                            <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                                  Height="30"
                                                  Padding="16,0"
                                                  Command="{Binding RefreshMarketCommand}">
                                                <StackPanel Orientation="Horizontal">
                                                    <materialDesign:PackIcon Kind="Refresh" 
                                                                           Width="16" 
                                                                           Height="16"
                                                                           VerticalAlignment="Center"
                                                                           Margin="0,0,6,0"/>
                                                    <TextBlock Text="重试" VerticalAlignment="Center" FontSize="13"/>
                                                </StackPanel>
                                            </Button>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Row="1"
                                          Background="{DynamicResource SurfaceBrush}"
                                          CornerRadius="6"
                                          Padding="16"
                                          Margin="0,0,0,12"
                                          Visibility="{Binding IsEmptyHintVisible, Converter={StaticResource BoolToVis}}">
                                        <StackPanel HorizontalAlignment="Center">
                                            <materialDesign:PackIcon Kind="Magnify" 
                                                                   Width="40" 
                                                                   Height="40"
                                                                   Foreground="{DynamicResource TextTertiaryBrush}"
                                                                   HorizontalAlignment="Center"
                                                                   Margin="0,0,0,10"/>
                                            <TextBlock Text="没有找到匹配的插件" 
                                                     FontSize="13"
                                                     Foreground="{DynamicResource TextSecondaryBrush}"
                                                     HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>

                                    <ScrollViewer Grid.Row="2"
                                                VerticalScrollBarVisibility="Auto"
                                                HorizontalScrollBarVisibility="Disabled">
                                        <ListBox BorderThickness="0"
                                               Background="Transparent"
                                               ItemContainerStyle="{StaticResource PluginLeftListItemStyle}"
                                               ItemsSource="{Binding LeftItems}"
                                               SelectedItem="{Binding SelectedItem}"
                                               ItemTemplate="{StaticResource PluginLeftItemTemplate}"/>
                                    </ScrollViewer>
                                </Grid>

                                <Border Grid.Column="2"
                                      Background="{DynamicResource SurfaceBrush}"
                                      CornerRadius="8"
                                      Padding="16">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Text="{Binding Detail.Title}"
                                                 FontSize="18"
                                                 FontWeight="Bold"
                                                 Foreground="{DynamicResource TextBrush}"
                                                 Margin="0,0,0,4"/>

                                        <TextBlock Grid.Row="1"
                                                 Text="{Binding Detail.Meta}"
                                                 FontSize="12"
                                                 Foreground="{DynamicResource TextSecondaryBrush}"
                                                 Margin="0,0,0,12"/>

                                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
                                            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                                  Height="32"
                                                  Padding="16,0"
                                                  Content="{Binding Detail.PrimaryActionText}"
                                                  Command="{Binding PrimaryActionCommand}"
                                                  IsEnabled="{Binding Detail.PrimaryActionEnabled}"
                                                  Visibility="{Binding Detail.PrimaryActionVisible, Converter={StaticResource BoolToVis}}"/>

                                            <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                                  Height="32"
                                                  Padding="16,0"
                                                  Margin="10,0,0,0"
                                                  Content="{Binding Detail.SecondaryActionText}"
                                                  Command="{Binding SecondaryActionCommand}"
                                                  Visibility="{Binding Detail.SecondaryActionVisible, Converter={StaticResource BoolToVis}}"/>

                                            <Button Style="{StaticResource MaterialDesignFlatButton}"
                                                  Height="32"
                                                  Padding="12,0"
                                                  Margin="10,0,0,0"
                                                  Content="主页"
                                                  Command="{Binding OpenHomeCommand}"
                                                  Visibility="{Binding Detail.HomeVisible, Converter={StaticResource BoolToVis}}"/>

                                            <Button Style="{StaticResource MaterialDesignFlatButton}"
                                                  Height="32"
                                                  Padding="12,0"
                                                  Margin="6,0,0,0"
                                                  Content="仓库"
                                                  Command="{Binding OpenRepoCommand}"
                                                  Visibility="{Binding Detail.RepoVisible, Converter={StaticResource BoolToVis}}"/>
                                        </StackPanel>

                                        <TabControl Grid.Row="3"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Margin="-8,0,-8,-8"
                                                    FontSize="12">
                                            <TabItem Header="详情"
                                                     Padding="8,2">
                                                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                    <TextBlock Text="{Binding Detail.Description}"
                                                             TextWrapping="Wrap"
                                                             Foreground="{DynamicResource TextSecondaryBrush}"
                                                             FontSize="13"
                                                             Margin="8,0,8,8"/>
                                                </ScrollViewer>
                                            </TabItem>

                                            <TabItem Header="输出"
                                                     Padding="8,2"
                                                     Visibility="{Binding Detail.OutputVisible, Converter={StaticResource BoolToVis}}">
                                                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                    <TextBox Text="{Binding Detail.Output}"
                                                             IsReadOnly="True"
                                                             BorderThickness="0"
                                                             Background="Transparent"
                                                             Foreground="{DynamicResource TextSecondaryBrush}"
                                                             FontSize="12"
                                                             Margin="8,0,8,8"
                                                             TextWrapping="Wrap"
                                                             VerticalScrollBarVisibility="Disabled"/>
                                                </ScrollViewer>
                                            </TabItem>
                                        </TabControl>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
